cmake_minimum_required(VERSION 3.3.0)
project(marp VERSION 0.1.0 LANGUAGES C CXX)

add_executable(marp main.cpp)

# Define tu archivo de datos
set(DATA_FILE "casos.txt")

# Lee el contenido del archivo en una variable de CMake (esto es para la configuración)
file(READ ${DATA_FILE} FILE_CONTENT)
message(STATUS "Contenido de config.txt (en tiempo de CMake): ${FILE_CONTENT}")

# Si quieres que sea parte de tu ejecutable
add_executable(MiApp main.cpp ${DATA_FILE}) # Esto funciona si tu C++ lo maneja
